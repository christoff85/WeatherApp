@model WeatherApp.ViewModels.WeatherViewModel

@{
    ViewBag.Title = "User panel";
}

<h2>@Model.Location</h2>
<div class="row">
    <div class="col-md-8">
        @Html.Partial("_DisplayWeather")
    </div>
</div>
